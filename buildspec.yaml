version: 0.2

phases:
  install:
    commands:      
      - echo Installing source NPM
      - npm install
  pre_build:
    commands:
      - echo Installing dependencies...     
  build:
    commands:
      - echo Build started...
      - npm run build
  post_build:
    commands:
      - aws s3 sync dist s3://auth-dev-test-filmio --acl 'public-read' --delete
